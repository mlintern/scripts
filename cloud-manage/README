To install things first do a 'bundle install'. You may need to do
'gem install bundle' first. This will take care of installing needed gems.

servers.rb - Connect to amazon API and retrieve servers, connect to servers
             and get hostnames. Output into output file usable by
             servers2dsh.rb

servers.sh - Shell functions that can use the output from servers.rb
servers.zsh - ZSH functions that can use the output from servers.rb

servers2dsh.rb - Parse output file from servers.rb into dsh group files

start-ec2.sh - Command for starting up EC2 instances. Good idea to run
               servers.rb and servers2dsh.rb after this

test_start.sh - Starts up a test environment, output into logfile
